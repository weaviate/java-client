dist: trusty
os: linux
language: java
jdk:
  - oraclejdk8

notifications:
  email:
    on_success: change

jobs:
  include:
    - stage: "Test"
      script:
        - jdk_switcher use oraclejdk8
        - mvn clean install
